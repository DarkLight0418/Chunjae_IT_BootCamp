MVC 모델 관련

1. 이해함
-> MVC 구조 대충(client -> controller -> service -> dao -> db
		      (req)        (dto)       (dto)  (dto)
		   ㄴ  JSP           <-         <-     <-

-> dto -> 편한 전달을 위한 파라미터 처리 (멤버변수, 생성자)
-> MVC 구조에 어떤 파일이 매핑되는지( ex.컨트롤러 -> 서블릿, ...)

2. 이해 못함
-> URL 주소 (서블릿 또는 JSP) 매핑해서 서버 실행 후 웹사이트 띄우는 거 과정
-> 

-----------------------
MVC 프로젝트 설정
-> dynamic web -> maven 프로젝트 전환
-> pom.xml에 종속성 추가
-> 서블릿 어노테이션을 통해서 진행