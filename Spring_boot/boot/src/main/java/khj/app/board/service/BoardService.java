package khj.app.board.service;
/*
import khj.app.board.domain.Board;
import khj.app.board.domain.PageInfo;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;

import java.util.List;

public interface BoardService {
    List<Board> findBoardList(PageInfo pageinfo);  // listB
    Board saveBoard(Board board);  // insertB
    void removeBoardBySeq(long seq);  // deleteB
    Board findBoardDetail(long seq);  // contentB
    //List<Board> updateBoard(Board board);  // modifyB
    int countAllBoards(); // countBoard
    int selectBoardPage(Pageable pageable);  // selectBoard

    List<Board> findBySubject(String subject);
    List<Board> findByContent(String content);
    List<Board> findByWriter(String writer);

    // Mybatis 전용
    int selectBoard(PageInfo pageInfo);
    int countBoard();

}
 */
