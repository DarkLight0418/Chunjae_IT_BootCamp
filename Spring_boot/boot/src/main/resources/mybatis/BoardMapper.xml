<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="khj.app.board.mapper.BoardMapper">
	<!-- 세미콜론 하면 안됨 -->

    <select id="listB" parameterType="PageInfo" resultType="Board">
        select * from BOARD order by SEQ desc limit #{startRow}, #{listLimit}
    </select>

	<select id="countBoard" resultType="int">
		select count(*) from BOARD
	</select>

	<!-- 파일... -->
	<!-- 
	<insert id="saveFile" parameterType="khj.board.domain.Attachment" useGeneratedKeys="true" keyProperty="seq">
		INSERT INTO ATTACHMENT
        (board_seq, original_name, saved_name, file_size, content_type, upload_date)
    VALUES
        (#{boardSeq}, #{originalName}, #{savedName}, #{fileSize}, #{contentType}, NOW())
	</insert>
	
	 -->
</mapper>